{
  "name": "buglog",
  "version": "1.0.0",
  "scripts": {
    "setup:app": "npm run setup:client && npm run setup:server",
    "setup:client": "cd buglog.client && npm i",
    "setup:server": "cd buglog.server && npm i",
    "start": "node buglog.server/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  },
  "dependencies": {
    "moment": "^2.29.1"
  }
}
